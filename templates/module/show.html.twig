{% extends 'base.html.twig' %}

{% block titrePage %}Module{% endblock %}

{% block contenuPage %}
    <h1>Module</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ module.id }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ module.code }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ module.titre }}</td>
            </tr>
            <tr>
                <th>Semestre</th>
                <td>{{ module.semestre }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('module_index') }}">Retour à la liste des modules</a>
    <br />
    <a href="{{ path('module_edit', {'id': module.id}) }}">Editer ce module</a>

    {{ include('module/_delete_form.html.twig') }}
{% endblock %}
